[pytest]
# services/task_manager FIRST: both it and the repo root contain a
# `common/app_common` package, and the task_manager copy is the one with
# settings, the runtime adapters and telegraf_config. The duplication is
# tracked as a merge-time cost in the decision memo's Proposal G.
pythonpath = services/task_manager .
testpaths = tests
