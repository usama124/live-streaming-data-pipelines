[pytest]
# services/task_manager FIRST: both it and the repo root contain a
# `common/app_common` package, and the task_manager copy is the one with
# settings, the runtime adapters, ch_naming and telegraf_config.
# services/ puts consumer_pool on the path the same way its image does.
pythonpath = services/task_manager services .
testpaths = tests
